<%= form_for(@palmitome_entry) do |f| %>
  <% if @palmitome_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@palmitome_entry.errors.count, "error") %> prohibited this palmitome_entry from being saved:</h2>

      <ul>
      <% @palmitome_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
