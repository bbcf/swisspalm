<%= form_for(@gene_name) do |f| %>
  <% if @gene_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gene_name.errors.count, "error") %> prohibited this gene_name from being saved:</h2>

      <ul>
      <% @gene_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
