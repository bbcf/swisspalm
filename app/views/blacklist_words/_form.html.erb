<%= form_for(@blacklist_word) do |f| %>
  <% if @blacklist_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blacklist_word.errors.count, "error") %> prohibited this blacklist_word from being saved:</h2>

      <ul>
      <% @blacklist_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
