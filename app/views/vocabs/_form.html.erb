<%= form_for(@vocab) do |f| %>
  <% if @vocab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vocab.errors.count, "error") %> prohibited this vocab from being saved:</h2>

      <ul>
      <% @vocab.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
<tr><td>
Data type
</td><td>
<% l = DataType.all.map{|e| [e.name, e.id]}.sort %>
<%= f.select :data_type_id, l %>
</td></tr>
<tr><td>
Name
</td><td>
<%= f.text_field :name %>
</td></tr>
<tr><td>
Description
</td><td>
<%= f.text_field :description %>
</td></tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
