<%= form_for(@annot) do |f| %>
  <% if @annot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@annot.errors.count, "error") %> prohibited this annot from being saved:</h2>

      <ul>
      <% @annot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
