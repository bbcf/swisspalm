<%= form_for(@phosphosite_type) do |f| %>
  <% if @phosphosite_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phosphosite_type.errors.count, "error") %> prohibited this phosphosite_type from being saved:</h2>

      <ul>
      <% @phosphosite_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
