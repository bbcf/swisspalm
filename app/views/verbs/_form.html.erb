<%= form_for(@verb) do |f| %>
  <% if @verb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@verb.errors.count, "error") %> prohibited this verb from being saved:</h2>

      <ul>
      <% @verb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">

<table>
<tr><td>
Verb type
</td><td>
<% l = VerbType.all.map{|e| [e.name, e.id]} %>
<%= f.select :verb_type_id, l %>
</td></tr>
<tr><td>
Name
</td><td>
<%= f.text_field :name %>
</td></tr>
<tr><td>
Description
</td><td>
<%= f.text_field :description %>
</td></tr>
</table>

    <%= f.submit %>
  </div>
<% end %>
