<%= form_for(@verb_type) do |f| %>
  <% if @verb_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@verb_type.errors.count, "error") %> prohibited this verb_type from being saved:</h2>

      <ul>
      <% @verb_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
<tr><td>
Name
</td><td>
<%= f.text_field :name %>
</td></tr>
<tr><td>
Description
</td><td>
<%= f.text_field :description %>
</td></tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
