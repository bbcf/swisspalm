<%= form_for(@protein_group) do |f| %>
  <% if @protein_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@protein_group.errors.count, "error") %> prohibited this protein_group from being saved:</h2>

      <ul>
      <% @protein_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
