<h1>Listing protein_groups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @protein_groups.each do |protein_group| %>
  <tr>
    <td><%= link_to 'Show', protein_group %></td>
    <td><%= link_to 'Edit', edit_protein_group_path(protein_group) %></td>
    <td><%= link_to 'Destroy', protein_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Protein group', new_protein_group_path %>
