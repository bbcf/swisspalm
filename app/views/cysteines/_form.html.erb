<%= form_for(@cysteine) do |f| %>
  <% if @cysteine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cysteine.errors.count, "error") %> prohibited this cysteine from being saved:</h2>

      <ul>
      <% @cysteine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
