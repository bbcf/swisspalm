<%= form_for(@technique_class) do |f| %>
  <% if @technique_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@technique_class.errors.count, "error") %> prohibited this technique_class from being saved:</h2>

      <ul>
      <% @technique_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
