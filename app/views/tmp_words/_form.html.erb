<%= form_for(@tmp_word) do |f| %>
  <% if @tmp_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tmp_word.errors.count, "error") %> prohibited this tmp_word from being saved:</h2>

      <ul>
      <% @tmp_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
