<h1>Listing history_studies</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @history_studies.each do |history_study| %>
  <tr>
    <td><%= link_to 'Show', history_study %></td>
    <td><%= link_to 'Edit', edit_history_study_path(history_study) %></td>
    <td><%= link_to 'Destroy', history_study, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New History study', new_history_study_path %>
