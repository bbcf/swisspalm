<%= form_for(@go_term) do |f| %>
  <% if @go_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@go_term.errors.count, "error") %> prohibited this go_term from being saved:</h2>

      <ul>
      <% @go_term.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
