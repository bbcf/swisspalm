<h1>Listing ref_isoforms</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ref_isoforms.each do |ref_isoform| %>
  <tr>
    <td><%= link_to 'Show', ref_isoform %></td>
    <td><%= link_to 'Edit', edit_ref_isoform_path(ref_isoform) %></td>
    <td><%= link_to 'Destroy', ref_isoform, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ref isoform', new_ref_isoform_path %>
