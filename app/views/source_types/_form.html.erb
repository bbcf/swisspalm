<%= form_for(@source_type) do |f| %>
  <% if @source_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@source_type.errors.count, "error") %> prohibited this source_type from being saved:</h2>

      <ul>
      <% @source_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
