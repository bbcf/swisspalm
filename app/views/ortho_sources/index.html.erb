<h1>Listing ortho_sources</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ortho_sources.each do |ortho_source| %>
  <tr>
    <td><%= link_to 'Show', ortho_source %></td>
    <td><%= link_to 'Edit', edit_ortho_source_path(ortho_source) %></td>
    <td><%= link_to 'Destroy', ortho_source, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ortho source', new_ortho_source_path %>
